# Convert strings to lists
string = 'Arpit'
lst = list(string)
print(lst)

str1 = 'Arpit Mallick is a good boy'
spt = (str1.split("a"))
print(spt)

# Convert back to strings using join()
print("a".join(spt))
